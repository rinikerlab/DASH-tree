# TODO: CUDA not installed by default
name: serenityff-charge-test
channels:
  - pytorch
  - pyg
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python = 3.10
  - pip
  # Dev
  - pytest
  - pytest-cov
  - codecov
  - ipython
  - pre-commit
  # Meta
  - conda-build
  # Science
  - pandas
  - pytables
  - numpy
  - numba
  # Cheminformatics
  - ambertools = 23.3
  - openff-toolkit = 0.10.0
  - rdkit
  # Machine Learning
  - pytorch = 2.0.0
  - pyg = 2.3.0
