# TODO: CUDA not installed by default
name: serenityff-charge-test
channels:
  - pytorch
  - pyg
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python = 3.10
  - pip
  # Dev
  - pytest
  - pytest-cov
  - codecov
  - IPython
  - pre-commit
  # Meta
  - conda-build
  # Science
  - pandas
  - pytables
  - numpy
  - numba
  # Chemie
  - ambertools = 23.3
  - openff-toolkit = 0.10.0
  - rdkit
  # Machine Learning
  - pytorch = 2.0.0
  - pyg = 2.3.0
